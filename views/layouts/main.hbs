<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{{yield "title"}}}</title> <!-- Change to handle inline title definitions -->
    <link rel="stylesheet" href="/css/styles.css">
    {{#if customCss}}<link rel="stylesheet" href="{{customCss}}">{{/if}}
</head>
<body>
    <header>
        <nav>
            {{#if loggedIn}}
                <p>Welcome, {{username}}! <a href="/dashboard">Dashboard</a> | <form action="/api/users/logout" method="POST" style="display: inline;"><button type="submit">Logout</button></form></p>
            {{else}}
                <a href="/api/users/login">Login</a> | <a href="/api/users/signup">Sign Up</a>
            {{/if}}
        </nav>
    </header>
    <main>
        {{{body}}}
    </main>
    <footer>
        <p>© 2024 Developer Blog. All rights reserved.</p>
    </footer>
</body>
</html>